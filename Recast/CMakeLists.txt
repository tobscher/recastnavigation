PROJECT(Recast)

CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

FILE(GLOB_RECURSE HEADERS Include/*.h)
FILE(GLOB_RECURSE SOURCES Source/*.cpp)

INCLUDE_DIRECTORIES(Include)

ADD_LIBRARY(Recast STATIC ${HEADERS} ${SOURCES})
